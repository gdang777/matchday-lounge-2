# ─────────────────────────────────────────────────────────────────────────────
# MatchDay Lounge — Environment Variables Template
# Copy this file to .env and fill in real values.
# NEVER commit .env to git. All production secrets live in Google Secret Manager.
# ─────────────────────────────────────────────────────────────────────────────

# ── Google Cloud ──────────────────────────────────────────────────────────────
GOOGLE_CLOUD_PROJECT=matchday-lounge-2
GOOGLE_CLOUD_REGION=northamerica-northeast1

# ── Firebase (client-side — safe to expose in web/mobile apps) ───────────────
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=matchday-lounge-2.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=matchday-lounge-2
VITE_FIREBASE_STORAGE_BUCKET=matchday-lounge-2.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
VITE_FIREBASE_APP_ID=your-firebase-app-id
VITE_FIREBASE_MEASUREMENT_ID=your-measurement-id

# ── Cloud Run API (server-side secrets — inject via Google Secret Manager) ───
# These MUST NOT appear in client-side code or Docker images.
# In production: gcloud secrets create <NAME> --data-file=...

ANTHROPIC_API_KEY=your-claude-api-key               # Claude API (Concierge + AI pre-screener)
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key    # Stripe — server-side only
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret     # Stripe webhook signature verification
APIFY_API_TOKEN=your-apify-token                    # Apify scraper (services/scraper)

# ── Firebase Admin SDK (server-side — Cloud Run + Functions) ─────────────────
# In production: use Application Default Credentials (ADC) via service account.
# For local dev only:
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# ── API URLs ──────────────────────────────────────────────────────────────────
# Local dev:
VITE_API_URL=http://localhost:8080
# Production (set after first Cloud Run deploy):
# VITE_API_URL=https://matchday-api-<hash>-nn.a.run.app

# ── Google Maps ───────────────────────────────────────────────────────────────
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key   # Restricted to your domains in GCP console

# ── App Config ────────────────────────────────────────────────────────────────
PORT=8080                                            # Cloud Run API port
NODE_ENV=development

# ── Stripe (client-side — publishable key only) ───────────────────────────────
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your-publishable-key
